resources:
  - ../tilt-dev

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: controller-manager
    patch: |
      - op: add
        path: /spec/template/spec/containers/0/args/-
        value: "--gateway-api-enabled"