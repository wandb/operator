telemetry:
  mode: "full"
  # Longer than the 1d default so incident data survives a support triage session.
  retentionPeriod: 3d
  scrape:
    kubeStateMetrics: true

victoria-metrics-operator:
  enabled: true

grafana-operator:
  enabled: true
